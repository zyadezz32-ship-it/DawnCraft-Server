name: DawnCraft Server (Easy Link)

on:
  workflow_dispatch:

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl unzip screen jq gnupg2

      - name: Setup Playit.gg
        run: |
          curl -SsL https://playit-cloud.github.io/ppa/key.gpg | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/playit.gpg >/dev/null
          echo "deb [signed-by=/etc/apt/trusted.gpg.d/playit.gpg] https://playit-cloud.github.io/ppa/data ./ " | sudo tee /etc/apt/sources.list.d/playit-cloud.list
          sudo apt update
          sudo apt install playit -y

      - name: Get Playit Claim Code
        run: |
          echo "------------------------------------------------"
          echo "!!! LOOK HERE FOR YOUR CLAIM CODE !!!"
          echo "------------------------------------------------"
          # This will start playit, show the claim code, and then we'll stop it to continue setup
          timeout 20s playit setup || true
          echo "------------------------------------------------"
          echo "Copy the code above and paste it into playit.gg"
          echo "------------------------------------------------"

      - name: Download and Install Forge & DawnCraft
        run: |
          wget -O forge-installer.jar https://maven.minecraftforge.net/net/minecraftforge/forge/1.18.2-40.2.17/forge-1.18.2-40.2.17-installer.jar
          java -jar forge-installer.jar --installServer
          rm forge-installer.jar
          wget -O DawnCraft_ServerPack.zip "https://mediafilez.forgecdn.net/files/7243/901/DawnCraft 2.0.16_hf Serverpack.zip"
          unzip -o DawnCraft_ServerPack.zip -d .
          rm DawnCraft_ServerPack.zip

      - name: Accept EULA and Configuration
        run: |
          echo "eula=true" > eula.txt
          echo "online-mode=false" > server.properties
          echo "view-distance=6" >> server.properties
          echo "max-tick-time=-1" >> server.properties

      - name: Run Server
        run: |
          echo "Starting Playit.gg in background..."
          playit run & 
          sleep 10
          echo "Starting Minecraft Server..."
          java -Xmx7G -Xms7G -Djava.awt.headless=true -XX:+UseG1GC @libraries/net/minecraftforge/forge/1.18.2-40.2.17/unix_args.txt nogui
